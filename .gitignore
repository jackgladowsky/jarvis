node_modules/
dist/
coverage/
*.log
.DS_Store

# Local env (templates only — runtime secrets live in ~/.jarvis/.env)
.env

# Local dev data sandbox (set JARVIS_DATA_DIR=$PWD/.jarvis-dev)
.jarvis-dev/
